# Copy to .env.local and fill in your values.
# This file is safe to commit; .env.local is gitignored.

# Base URL of your PHP backend (no trailing slash)
VITE_API_BASE_URL=https://api.your-domain.com
